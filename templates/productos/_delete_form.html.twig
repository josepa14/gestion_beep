<form method="post" action="{{ path('app_productos_delete', {'id': producto.id}) }}" onsubmit="return confirm('Quieres borrar este producto?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    <button class="btn btn-danger mt-2 mb-5">Borrar</button>
</form>
